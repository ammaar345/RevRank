<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all theming (RevRankTheme); platform parent avoids a
         Material Components dependency just for this wrapper style. -->
    <style name="Theme.RevRank" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:colorBackground">#0A0A0A</item>
        <item name="android:windowBackground">#000000</item>
        <item name="android:statusBarColor">#000000</item>
        <item name="android:navigationBarColor">#000000</item>
    </style>

    <style name="Theme.TripLaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- Show a launch screen that matches the app's eventual theme. -->
        <item name="android:windowBackground">@drawable/launch_screen</item>
    </style>
</resources>